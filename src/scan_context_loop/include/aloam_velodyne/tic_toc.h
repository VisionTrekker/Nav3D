#pragma once

// Compatibility include for the legacy Scan Context header.  The package
// carries the unchanged timer implementation under its own include namespace.
#include "../scan_context_loop/tic_toc.h"
